import styled from 'styled-components'

const StyledHeading = styled.h2`
    font-size: calc(10px + 5vh);
    font-weight: normal;
    margin-bottom: 1vh;
`

export default function CharacterDesign(){
    return(
        <>
            <StyledHeading>Character Design</StyledHeading>
        </>
    )
}