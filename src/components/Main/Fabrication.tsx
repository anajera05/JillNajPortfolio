import styled from 'styled-components'

const StyledHeading = styled.h2`
    font-size: calc(10px + 5vh);
    font-weight: normal;
    margin-bottom: 1vh;
`

export default function Fabrication(){
    return(
        <>
            <StyledHeading>Fabrication</StyledHeading>
        </>
    )
}